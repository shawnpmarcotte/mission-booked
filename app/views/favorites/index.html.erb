<h1>Favorites</h1>

<ul>
<% @favorites.each do |favorite|%>
  <li>
    <%= link_to favorite.event.name, event_path(favorite.event.id)%>
    <%= link_to "❌", favorite_path(favorite.event.id), method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
<% end %>
</ul>
